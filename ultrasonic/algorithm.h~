#ifndef ALGORITHM_H
#define ALGORITHM_H
#include "header.h"
typedef ulong QuickSortType; 
void quicksort(QuickSortType *array, int n);
void copyArr(QuickSortType *from,QuickSortType *to,int n);
long abs(long a);
long abs2(long a,long b);
char sign(long a);
ulong sum(ulong *arr,uchar size);
#endif

